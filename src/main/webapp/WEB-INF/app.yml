## YAML Template.
runtime: java
env: flex
runtime_config:  # Optional
  jdk: openjdk8
handlers:
- url: /.*
  script: this field is required, but ignored
manual_scaling:
  instances: 1
cloud_sql_instances: supermarkethelpingyou:asia-southeast1:smhu=tcp:1433